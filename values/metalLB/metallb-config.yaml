apiVersion: v1
kind: ConfigMap
metadata:
  namespace: metallb-system
  name: config
data:
  config: |
    address-pools:
    - name: $DEFAULT_NETWORK/$DEFAULT_NETMASK
      protocol: layer2
      addresses:
      - $START_IP-$END_IP
