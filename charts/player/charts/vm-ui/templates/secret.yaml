apiVersion: v1
kind: Secret
metadata:
  name: {{ include "vm-ui.fullname" . }}
  labels:
    {{- include "vm-ui.labels" . | nindent 4 }}
type: Opaque
stringData:
{{- range $key, $val := .Values.env }}
  {{ $key }}: {{ $val | quote }}
{{- end }}