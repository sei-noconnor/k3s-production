apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "player-ui.fullname" . }}-test-connection"
  labels:
    {{- include "player-ui.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "player-ui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
