apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "learninglocker-ui.fullname" . }}-test-connection"
  labels:
    {{- include "learninglocker-ui.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "learninglocker-ui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
